name: Continuos Integration
on:
  push:
    branches:
      - develop
      - "feature/**"
    
  pull_request:
    branches:        
      - develop
jobs:
  backend:
    name: Backend Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        